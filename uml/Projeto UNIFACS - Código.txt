
monte as classes model, repository e as entidades seguindo essas classes: mapeadas classDiagram
utilize logs ao inves de system.out


classDiagram
    class Usuario {
        +int id
        +string nome
        +string email
        +string perfilAcesso
        +cadastrar()
        +gerenciarPermissoes()
    }

    class Projeto {
        +int id
        +string nome
        +date dataInicio
        +date dataFim
        +string status
        +float progresso
        +criarProjeto()
        +gerarRelatorio()
        +ajustarRota()
    }

    class Equipe {
        +int id
        +string nomeEquipe
        +list usuario
        +alocarNoProjeto()
    }

    class Tarefa {
        +int id
        +string descricao
        +string status
        +int horasEstimadas
        +int horasTrabalhadas
        +distribuir()
        +atualizarStatus()
        +registrarHoras()
    }

    class Dashboard {
        +list indicadores
        +list portfolioProjetos
        +visualizarDashboards()
        +tomarDecisao()
    }




    %% Relacionamentos

    Projeto tem equipe atribuida
    Equipe tem (usuarios, tarefas)

    Dashboard serve para gerenciar os relacionamentos
    Ex:
    usuario:
       admin - cadastra, edita e exclui usuarios
       gerente - cadastra, edita e exclui (equipe e projeto) atribuindo os relacionamentos
       usuarios possuem tarefas e pode visualizar as tarefas e editar  e deletar

    Todos podem visualizar no dashboard mas somente os de grupo especificos conseguem fazer mudanças


